// Deprecated — Arabic locale removed per project decision (FR only).
// Keeping the file as a no-op stub so any stale import doesn't crash.
const LanguageSwitcher = () => null;
export default LanguageSwitcher;
